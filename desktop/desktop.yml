---
- hosts: localhost
  pre_tasks:
    - name: install basics
      become: yes
      apt: name={{ item }}
      with_items:
        - curl
        - vim
        - jq
        - python-pip
  roles:
    - git
    - chrome
    - sublime
    - docker
    - autojump
    - clojure
    - elixir
    - nodejs
